import fs from 'node:fs';import path from 'node:path';
const root=path.resolve('outputs/attachment-audit');const read=n=>JSON.parse(fs.readFileSync(path.join(root,n),'utf8'));const write=(n,v)=>fs.writeFileSync(path.join(root,n),`${JSON.stringify(v,null,2)}\n`);
const fixes=new Map([
 ['BROD 3|Muzzle|1',{control:35}],['BROD 3|Grip|17',{mobility:60}],['DB-12|Grip|6',{mobility:60}],['GRT-BC|Grip|16',{mobility:60}],
 ['KV9|Muzzle|1',{control:60}],['KV9|Grip|15',{control:60}],
 ['M433|Muzzle|1',{hipfire:40}],['M433|Grip|16',{precision:20,control:40,mobility:52,adsTimeMs:250}],['M433|Laser|55',{mobility:52}],
 ['QBZ-192|Grip|8',{mobility:60}],['RPK-74M|Muzzle|1',{control:60}],['SOR-300SC|Grip|13',{mobility:60}],
]);
const review=read('attachment-screenshot-review.json');const changed=[];
for(const row of review.records.filter(r=>r.stats)){const key=`${row.weaponName}|${row.attachmentType}|${row.source.captureOrder}`;const fix=fixes.get(key);if(!fix)continue;const updates={};for(const [field,value]of Object.entries(fix))if(row.stats[field]!==value){updates[field]={before:row.stats[field],after:value};row.stats[field]=value;}if(Object.keys(updates).length){row.mappingReviewStatus='visually-checked';row.notes=[...new Set([...(row.notes??[]),'Type-specific None baseline visually corrected from the detailed screenshot panel on 2026-07-28; leading-zero segmentation had caused comparison contradictions.'])];changed.push({key,path:row.source.currentPath,updates});}}
if(changed.length!==12)throw new Error(`Expected 12 corrected baseline records, got ${changed.length}`);review.generatedAt=new Date().toISOString();write('attachment-screenshot-review.json',review);
const manual=read('manual-review-overrides.json');for(const item of changed){const row=review.records.find(r=>`${r.weaponName}|${r.attachmentType}|${r.source.captureOrder}`===item.key);let o=manual.overrides.find(x=>x.sourcePath&&path.resolve(x.sourcePath).toLowerCase()===path.resolve(row.source.currentPath).toLowerCase());if(!o){o={weaponName:row.weaponName,attachmentType:row.attachmentType,attachmentName:row.attachmentName,sourcePath:row.source.currentPath,sourceFilename:path.basename(row.source.currentPath),updates:{},evidence:[]};manual.overrides.push(o);}o.updates={...(o.updates??{})};for(const [field,{after}]of Object.entries(item.updates))o.updates[field]=after;o.mappingReviewStatus='visually-checked';o.evidence=[...new Map([...(o.evidence??[]),{kind:'direct-none-baseline-stat-panel-review',reviewDate:'2026-07-28'}].map(x=>[JSON.stringify(x),x])).values()];}manual.generatedAt=new Date().toISOString();write('manual-review-overrides.json',manual);console.log(JSON.stringify({corrected:changed.length,changed},null,2));
