# BF6 Weapon Analyzer — Claude Guidelines

## Git Workflow

- **Do not push commits until explicitly told to.** Commit freely, but always wait for the user to say "push" or "go ahead and push" before running `git push`.
